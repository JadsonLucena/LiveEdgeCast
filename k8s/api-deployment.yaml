apiVersion: apps/v1
kind: Deployment
metadata:
  name: rtmp-controller
  namespace: media
spec:
  replicas: 1
  selector:
    matchLabels: { app: rtmp-controller }
  template:
    metadata:
      labels: { app: rtmp-controller }
    spec:
      serviceAccountName: rtmp-controller
      containers:
        - name: controller
          image: liveedgecast-controller:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          env:
            - name: PYTHONUNBUFFERED
              value: "1"
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 2
            periodSeconds: 3
