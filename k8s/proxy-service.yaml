apiVersion: v1
kind: Service
metadata:
  name: rtmp-proxy
  namespace: media
  labels:
    app: rtmp-proxy
spec:
  type: NodePort
  selector: { app: rtmp-proxy }
  ports:
    - name: rtmp
      port: 1935
      targetPort: 1935
      nodePort: 31935  # Fixed NodePort for external RTMP access
      protocol: TCP
    - name: rtmp-metrics
      port: 9114
      targetPort: 9114
      protocol: TCP
