apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rtmp-proxy-metrics
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames: ["media"]
  selector:
    matchLabels:
      app: rtmp-proxy
  endpoints:
    - port: http
      interval: 10s
      path: /metrics
