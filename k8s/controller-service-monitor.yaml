apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: controller-metrics
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames: ["media"]
  selector:
    matchLabels:
      app: controller
  endpoints:
    - port: http
      interval: 10s
      path: /metrics
